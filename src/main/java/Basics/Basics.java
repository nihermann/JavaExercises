package Basics;

/**
 * @Author Nicolai Hermann
 * Um die Tests laufen zu lassen, musst du entweder alle Methoden deklarieren und einen konstanten Wert zurückgeben,
 * oder die Tests für die folgenden Aufgaben auskommentieren. Vergiss dann halt nicht sie wieder einzukommentieren.
 */
public class Basics {
    /**
     * Schreiben Sie ein neues Konsolenprogramm. In einem Dreieck gilt der Innenwinkelsatz. Das Programm
     * soll zwei Winkel einlesen und dann den dritten Winkel ausgeben.
     * Überlegen Sie sich anschließend, ob das Programm tatsächlich immer funktioniert. Kann man als
     * Nutzer Fehler provozieren? Sorgen Sie für entsprechende Fehlerbehandlung und geben sie -1 zurück falls die
     * Eingabe ungültig ist.
     * @param winkel1
     * @param winkel2
     * @return winkel3
     */
    public static float innenWinkelSumme(float winkel1, float winkel2){
        // YOUR CODE HERE
        return 0;
    }

    /** Fahrenheit in Celcius
     * Eine Temperatur in Fahrenheit kann in Celsius umgerechnet werden, indem 32 abgezogen wird und
     * das Ergebnis mit 5/9 multipliziert wird:
     * ◦C = (◦F − 32) · 5/9
     * Rechnen Sie 89.6 ◦F sowie 212 ◦F in ◦C um. Schreiben Sie dann ein Konsolenprogramm, dass eine
     * Temperatur in ◦F einliest, in ◦C umrechnet und ausgibt. Die Methode dazu soll fahrenheitInCelcius heißen,
     * ein ein argument nehmen und die übergebende Zahl in Fahrenheit, umgerechnent in Celcius zurückgeben.
     */

    // YOUR CODE HERE



    /** Fibonacci - Endlich viele Schritte
     * Die Zahlen der Fibonaccifolge lassen sich wie folgt für n ≥ 2 definieren:
     * fn = fn−1 + fn−2
     * Berechnen Sie per Hand, ausgehend von f0 = 0 und f1 = 1 die Folgenglieder f2 bis f7 der Fibonaccifolge. Wie viele Werte benötigen Sie für die Berechnung der jeweils nächsten Zahl?
     * Schreiben Sie ein neues Konsolenprogramm, das drei Zahlen einliest. Interpretieren sie die erste Zahl
     * als den Index des letzten Folgenglieds und die anderen beiden Zahlen als Startzahlen der Folge. Ihr
     * Programm soll die entsprechenden Folgenglieder ausgeben.
     * Nenne die Methode fib, welche 3 Argumente besitzt für den Endindex, Anfangswert 1 und Anfangswert 2.
     */

    // YOUR CODE HERE


    /** Arrays 1
     * Schreibe eine Methode, welche ein int array als Parameter übergeben bekommt und
     * anschließend ein neues array in verkehrter Reihenfolge zurückgibt.
     * Der Name der Methode soll 'revert' heißen.
     */

    // YOUR CODE HERE


    /** Arrays 2
     * Als nächstes wollen wir das gleiche für 2-dimensionale arrays lösen.
     * Überlade dafür die bereits geschriebene revert methode für 2D arrays.
     * Solltest du nicht wissen was überladen bedeutet, oder wie man 2D arrays erstellt,
     * Google ist dein bester Freund :)
     * Bsp: {{1, 2, 3},         {{9, 8, 7},
     *       {4, 5, 6},    =>    {6, 5, 4},
     *       {7, 8, 9}}          {3, 2, 1}}
     *
     * @param arr2D
     */

    // YOUR CODE HERE

}
